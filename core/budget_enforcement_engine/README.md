# Mezzanine Budget Enforcement Engine

Memory-default fail-closed budget enforcement for Phase D.

The engine evaluates preflight, append, stream, runtime admission, and
reconciliation loci. Hard-cap overrides require an explicit budget override
permission, bounded reason ref, and bounded duration.
