# Mezzanine Eval Engine

Deterministic golden-dataset eval harness for replay-bound prompt, model,
policy, guard, and memory variants. Cases and verdicts carry refs and hashes,
not raw eval or model payloads.
