# Layout

The root is tooling-only.

Current structure:

```text
build_support/
core/mezzanine_core/
docs/
packaging/weld/mezzanine_core/
```

Future growth is expected in `core/`, `surfaces/`, `bridges/`, and `apps/`,
but the root should remain a workspace owner rather than a runtime home.
